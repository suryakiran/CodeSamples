#include <std.hxx>
#include <qt.hxx>
#include <QCleanlooksStyle>

#include <@WindowName@.hxx>

int main (int argc, char** argv)
{
  QApplication app (argc, argv);
  app.setStyle(new QCleanlooksStyle);

  Window* w = new @WindowName@;
  w->show();
  qApp->installEventFilter(w);

  app.exec();
}
